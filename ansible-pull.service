# /etc/systemd/system/ansible-pull.service
[Unit]
Description=Bootstrap using Ansible

[Service]
Type=oneshot
ExecStart="ansible-pull -U https://gitlab.com/goshansp/infrastructure.git -i localhost, bootstrap.yml"

[Install]
WantedBy=multi-user.target